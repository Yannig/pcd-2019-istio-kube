apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pcd-2019.fullname" . }}-test-connection"
  labels:
{{ include "pcd-2019.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pcd-2019.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
