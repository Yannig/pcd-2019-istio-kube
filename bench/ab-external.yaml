apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: ab-external
  name: ab-external
spec:
  selector:
    matchLabels:
      app: ab-external
  template:
    metadata:
      labels:
        app: ab-external
    spec:
      containers:
      - image: nicolaka/netshoot
        name: netshoot
        command:
          - "sh"
          - "-c"
          - "while sleep 0.`echo $RANDOM%15 | bc` ; do curl -v https://mailhog-ingress.pcd-2019.aios.sh 2>&1 > /dev/null | grep '^< HTTP'; done"
